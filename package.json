{
  "name": "todo-txt",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal todo.txt task manager — plain HTML, CSS and JavaScript, no build step.",
  "scripts": {
    "test": "node test/run.js"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
